{/* Label.stories.mdx */}

import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import * as Button from './button';

<Meta
    title="Button"
    component={Button.Root}
    argTypes={{
        size: {
            name: 'size',
            description: 'Checkbox Size',
            control: {
                type: 'select',
            },
        },
        color: {
            name: 'color',
            description: 'Button Color',
            control: {
                type: 'select',
            },
        },
    }}
/>

export const Template = (args) => (
    <Button.Root size={args.size} {...args}>
        {args.children} Button
    </Button.Root>
);

export const SizeTemplate = (args) => (
    <div style={{ gap: '1rem', display: 'flex', flexWrap: 'wrap' }}>
        <Button.Root {...args} color="default">
            default
        </Button.Root>
        <Button.Root {...args} color="secondary">
            Secondary
        </Button.Root>
        <Button.Root {...args} color="tertiary">
            Tertiary
        </Button.Root>
        <Button.Root {...args} color="error">
            Error
        </Button.Root>
        <Button.Root {...args} color="warn">
            Warning
        </Button.Root>
        <Button.Root {...args} color="info">
            Information
        </Button.Root>
        <Button.Root {...args} color="success">
            Success
        </Button.Root>
    </div>
);

# Label

With `MDX`, we can define a story for `Label` right in the middle of our Markdown documentation.

# Root

<ArgsTable of={Main} />

<Canvas>
    <Story
        name="Main"
        args={{
            size: 'large',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="All Colors"
        args={{
            size: 'large',
        }}
    >
        {SizeTemplate.bind({})}
    </Story>
</Canvas>
