{/* Label.stories.mdx */}

import { Canvas, Meta, Story } from '@storybook/addon-docs';
import DatePicker, { Trigger } from './datepicker';
import DatePickerTwo from './components/dateTime';
<Meta title="Datepicker" component={DatePicker} />;

# Flex-Grid

With `MDX`, we can define a story for `Label` right in the middle of our Markdown documentation.

<Canvas>
    <Story name="Main">
        <DatePicker time={new Date(2023, 3, 4)} />
    </Story>
    <Story name="Popup">
        <Trigger time={new Date(2023, 3, 4)}></Trigger>
    </Story>
    <Story name="Date-Time">
        <DatePickerTwo />
    </Story>
</Canvas>
