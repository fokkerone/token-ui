<!-- Label.stories.mdx -->

import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import * as Avatar from './avatar';

<Meta
  title="Avatar"
  component={Avatar.Root}
  argTypes={{
    url: {
      name: 'url',
      description: 'lalalo',
      control: {
        type: 'text',
      },
    },
    delayMS: {
      name: 'delayMS',
      description: 'Delay in ms showing fallback',
      control: {
        type: 'text',
      },
    },
    bg: {
      name: 'backgroundcolor',
      description: 'Avatar Background Color',
      control: {
        type: 'color',
      },
    },
    color: {
      name: 'Color',
      description: 'Avatar Text Color',
      control: {
        type: 'color',
      },
    },
    size: {
      name: 'size',
      description: 'Avatar Size',
      control: {
        type: 'select',
      },
    },
    type: {
      name: 'type',
      description: 'lalalo',
      control: {
        type: 'select',
      },
    },
    fallback: {
      name: 'M-C',
      description: 'Text shown by Badge',
      control: {
        type: 'text',
      },
      table: {
        type: {
          summary: 'The label contents',
          detail: 'Text displayed by the Badge',
        },
      },
    },
  }}
/>

export const Template = (args) => (
  <Avatar.Root
    size={args.size}
    type={args.type}
    color={args.color}
    bg={args.bg}
  >
    <Avatar.Image src={args.url} />
    <Avatar.Fallback delayMS={args.delayMS}>{args.fallback}</Avatar.Fallback>
  </Avatar.Root>
);

# Label

With `MDX`, we can define a story for `Avatars` right in the middle of our Markdown documentation.

## Root

<ArgsTable of={Avatar.Root} />

## Fallback

<ArgsTable of={Avatar.Image} />

<Canvas>
  <Story
    name="Main"
    args={{
      delayMS: '1200',
      fallback: 'DC',
      url: 'https://placeimg.com/250/250/people',
      type: 'rounded',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
